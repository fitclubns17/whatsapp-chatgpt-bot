# Conteúdo idealizado para o requirements.txt com base no que o user já tem
requirements_content = """flask==3.1.0
requests==2.32.3
openai==1.68.2
python-dotenv==1.1.0
"""

# Guardar o conteúdo idealizado num ficheiro para referência futura
requirements_path = Path("/mnt/data/requirements_deploy.txt")
requirements_path.write_text(requirements_content)

requirements_path